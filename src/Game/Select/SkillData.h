#pragma once
#include <System/Scene.h>

namespace Select {
USING_PTR(SkillData);

class SkillData : public Object
{
public:
    BP_OBJECT_DECL(SkillData, u8"Select の SkillData");

private:
};

}    // namespace Select
